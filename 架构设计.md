# 架构设计 — Star 仓库清理建议器

> 目标：数据层/AI 层/执行层严格分离——AI 只做"判断"，执行只做"动作"，缓存只做"状态"。

## 1. 总体分层

```
┌─────────────────────────────────────────────┐
│              main.py (CLI 编排)             │
├──────────┬──────────┬──────────┬────────────┤
│ gh_data  │ analyze  │ actions  │ store     │
│ GitHubAPI│ 规则+LLM │ unstar/   │ SQLite    │
│ (读)     │ (判断)   │ star     │ 缓存与状态 │
└──────────┴──────────┴──────────┴────────────┘
```

依赖方向：`main → gh_data / analyze / actions`；`analyze → store`（读历史，防重复判断）；`actions → store`（写状态）。
**禁止**：`gh_data` 内部不 import `analyze/actions`；LLM 密钥只进 `analyze`。

## 2. 接口契约

### 2.1 `gh_data.py`（只读数据源）
```python
@dataclass
class RepoInfo:
    full_name: str
    html_url: str
    stars: int
    language: str | None
    pushed_at: datetime
    archived: bool
    open_issues: int
    description: str | None
    repo_created_at: datetime
    fetched_at: datetime

@dataclass
class FetchResult:
    repos: list[RepoInfo]
    rate_remaining: int
    rate_reset_at: datetime | None

def get_starred(username: str, token: str | None, *,
                per_page: int = 100) -> Iterator[RepoInfo]
    # 分页迭代；打印进度；Token 缺省= 60/小时匿名（提示）

def fetch_error_classes() -> tuple[type[Exception], ...]
    # (GithubRateLimitError, GithubException) 统一异常出口
```

### 2.2 `store.py`（SQLite，唯一持久层）
```python
SCHEMA = """
CREATE TABLE IF NOT EXISTS stars(
  full_name TEXT PRIMARY KEY,
  fetched_at INTEGER,           -- 缓存时间
  data_json TEXT,               -- RepoInfo 序列化（供离线重算）
  verdict TEXT,                 -- keep/unstar/revisit/null
  reason TEXT,
  score REAL,                   -- LLM/规则综合分 0-100
  unstarred_at INTEGER,         -- null=未处理
  decision_user TEXT            -- 'keep'|'unstar'|''（人工确认结果）
)
"""
class StarStore:
    def __init__(self, path: str = "~/.starclean.db")
    def get_cached(self, username) -> list[RepoInfo]      # fetched_at 24h 内
    def upsert_repos(self, repos: list[RepoInfo])
    def save_verdicts(self, verdicts: list[Verdict])     # verdict+reason+score
    def mark_unstarred(self, repo: str, at: datetime)
    def list_unstarred(self) -> list[str]
    def list_pending(self) -> list[str]
```

### 2.3 `analyze.py`（判断层，只读判定）
```python
@dataclass
@dataclass
class Verdict:
    repo: str
    verdict: str            # "keep" | "unstar" | "review"
    score: int              # 0-100
    reason: str             # 一句话（含数据依据，如"last push 2.1y ago"）
    source: str             # "rule" | "llm"

# 本地硬规则（可离线！不依赖 LLM）：
RULE_EXPIRED_DAYS = 730      # >2年无活动 → 分 base 降低
RULE_ARCHIVED_DAY  = 1       # archived → unstar 得分升高
RULE_MIN_STARS_ACTIVE = 30   # <30 star 且 2年无 → 若无 forks 则 unstar

def rule_verdict(repo: RepoInfo) -> Verdict | None  # 输出确定性: keep/unstar/审查
def llm_verdict(repo: RepoInfo) -> Verdict           # 调用外部模型（JSON契约见下）
def combined(repo: RepoInfo) -> Verdict              # 规则优先；规则 None 时走 llm
def summarize(candidates: list[Verdict]) -> str      # 生成 Markdown 报告
```

**LLM JSON 契约**（非严格结构重试一次）：
```json
{"score": 32, "verdict": "unstar", "reason": "最后提交 2024-03，900 天无活动且无分支; 建议清理"}
```
`verdict ∈ {"keep","unstar","revisit"}`，`score∈[0,100]`。

### 2.4 `actions.py`（唯一写 GitHub 处）
```python
class ActionError(Exception)

def unstar(repo: str, token: str, *, dry_run: bool = True) -> bool
def restar(repo: str, token: str) -> bool                 # rollback
def unstar_many(repos: list[str], token, *, batch_size=5, sleep_s=0.5,
                confirm_fn: Callable[[list[str]], bool]) -> dict[str, str]
    # {repo: "done"|"skipped"(已处理)|"failed"|"cancelled"}
```
- `unstar_many` 先调 `confirm_fn(清单)` 展示全量→用户确认→执行
- 幂等：`store.list_unstarred()` 内跳过
- 限速：每批 sleep 0.5s；403 时急停并提示

### 2.5 CLI 契约
```
starclean scan [--user u] [--token env:GITHUB_TOKEN] [--refresh] [--no-llm] [--json]
starclean report [--to report.md]
starclean decide? unstar --dead|--stale|--all [--yes] [--dry-run]
starclean undo [--repo x]
```
- 默认 `scan + report` 只读；`unstar` 必须有 `--yes` 或交互确认

## 3. 数据流
```
scan: gh_data.get_starred → store.upsert(缓存更新)
   → for each：combined()→ store.apply_verdicts
   → 输出 shortlist（按 score 升序）→ 命令决定是否 unstar
unstar: args筛选 → 展示确认 → unstar_many → store.mark_unstarred
rollback: list_unstarred → restar → 清除标记
```

## 4. 并行开发切分
| 任务 | 模块 | 依赖 |
|---|---|---|
| A | `gh_data.py`+测试 | 无 |
| B | `store.py`+测试 | 无 |
| C | `actions.py`+测试 | B(桩) |
| D | `analyze.py` 规则模型+LLM | A(类型), B |
| E | `main.py` CLI 集成 | A-D |
A/B 并行 → C/D 并行 → E。

## 5. 接口核对清单
- [ ] `store.get_cached` 24h 内不二次拉 API
- [ ] `rule_verdict` 对"2 年无 push + archived" 必返回非 None
- [ ] `unstar_many` 对所有就绪执行前必然调用 confirm_fn
- [ ] 两次 `unstar` 同批只改一次（幂等）
- [ ] `main.py` 无 `--yes` 时绝不发起 unstar 网络请求