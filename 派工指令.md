# 派工指令 — Star 仓库清理建议器（StarGrave）

> 生成时间：2026-08-08，基于 projects/04-Star清理建议器/ 下四份文档打包。
> 用法：本文件可直接粘贴给独立 AI；若对方无法访问本地文件，见文末「附录：四份文档全文」。

【角色】你是独立软件工程师，负责从零实现一个工具，全程自主决策，不向用户提问。

【项目定位】
- 项目名：Star 仓库清理建议器（StarGrave）
- 一句话描述：扫描你 GitHub 上所有 Star 的仓库，用 AI 告诉你"哪些已经死了、哪些值得看"——一键 unstar 死项目
- 所属目录：projects/04-Star清理建议器/
- 文档位置：
  1. projects/04-Star清理建议器/思路.md —— 产品背景、证据、边界与免责
  2. projects/04-Star清理建议器/开发规范.md —— 硬性规范（接口、安全、测试、交付物）
  3. projects/04-Star清理建议器/开发规划.md —— 分阶段计划与验收清单
  4. projects/04-Star清理建议器/架构设计.md —— 模块划分、接口契约、并行切分

【工作方式】
1. 开工前，先完整阅读上述 4 份文档，如有矛盾，以「架构设计.md > 开发规范.md > 开发规划.md > 思路.md」为优先级顺序。
   **已知文档差异（已裁决，直接按此实现，勿再纠结）**：
   - 数据层入口函数：开发规范.md 写 `list_starred(username)`，架构设计.md 定为 `get_starred(username, token, *, per_page=100)` → **以架构设计.md 为准**（`list_starred` 可作兼容别名，非必需）。
   - verdict 枚举：开发规范.md 第 3 节混用 `dead|stale|active|watch` 与 `keep|unstar|revisit`；架构设计.md 定为 `verdict ∈ {"keep","unstar","revisit"}`，`score∈[0,100]` → **以架构设计.md 为准**（输出展示层可用中文标签，但 JSON 字段值必须严格用该枚举）。
2. 按「架构设计.md」第 4 节并行切分表（A `gh_data.py` / B `store.py` / C `actions.py` / D `analyze.py` / E `main.py` 集成）实现。**若只派你一个 Agent 做全量**，则按 A→B→C→D→E 顺序完成全部模块；**绝不修改接口契约**（dataclass/函数签名/JSON schema/CLI 参数/退出码）。
3. 依次完成「开发规划.md」中的 Phase 1→2→3（→5；Phase 4 Web 单页为可选，若工期不允许可跳过并在交付说明标注），每阶段末运行该阶段验证命令并记录结果。
4. 只使用文档中声明的依赖：`PyGithub`、`rich`、`sqlite3`（标准库）；`openai` 为可选（LLM 判断可用 `requests`/标准库调 HTTP 也行，但需在 README 说明）。新增任何第三方依赖前，先说明理由（文档未允许的一律不加）。
5. 全程不向用户提问；遇到歧义 → 选择文档规定的最保守/最安全方案，并在交付说明中标注。

【安全红线（适用全部项目）】
- 密钥/Token 一律从环境变量读取（`GITHUB_TOKEN`、`LLM_API_KEY`），绝不硬编码、绝不写入任何文件、绝不入日志、绝不进 README 示例
- 本工具对用户数据默认只读；涉及写盘（unstar/star/rollback）必须遵循文档的 dry-run/确认/回滚要求：`unstar` 调用前必须打印整份清单并经 `confirm_fn` 确认；`main.py` 无 `--yes` 时绝不发起 unstar 网络请求
- 不得发送用户原始敏感数据到第三方；发往 LLM 的只是仓库公开摘要（stars/语言/pushed/archived/desc 前 200 字），不含 Token
- 幂等与可撤销：已 unstar 的（SQLite 记录 `unstarred_at`）跳过并提示"已处理"；`undo` 用 `restar` 恢复

【代码规范】
- 语言/依赖/目录结构严格按「开发规范.md 第1节」：Python 3.9+，`main.py`（CLI）/ `gh_data.py`（API 拉取）/ `analyze.py`（AI 判断）/ `actions.py`（执行）/ `store.py`（SQLite）
- 不加文档中未列明的注释（代码里维持最小注释，必要时只加文档字面要求）
- 所有公开函数按契约命名，docstring 只写用途一句话
- `gh_data` 内部不 import `analyze/actions`；LLM 密钥只进 `analyze`

【测试要求】
- 每个模块必须有对应测试文件：`tests/test_gh_data.py`（mock PyGithub：分页、缓存、限流跳过）、`tests/test_analyze.py`（JSON 契约、`keep/unstar/revisit` 边界、LLM 返回畸形 JSON 时优雅降级标记 `unknown` 且不执行动作）、`tests/test_actions.py`（unstar 幂等、unstar 记录），`pytest tests/ -q` 全绿
- 测试不得依赖网络（可用 mock/stub；API 调用类用假响应）
- 集成测试仅用于验证接口（CLI/服务初始化）不跑长链路

【完成后交付内容（按此格式输出）】
1. 变更清单：实现了哪些模块，各阶段验证结果 + 证据（命令输出文本）
2. 使用示例：至少 3 个命令/调用示例（含关键输出）
3. 部署/运行说明：依赖安装命令、环境变量设置、测试运行命令
4. 与规范偏差表：若有偏离文档决策的项，列明与理由
5. 已知限制与本阶段未做的功能（按规划勾选：Phase 4 Web 是否跳过等）

【验收清单】（完成时逐项自检勾选，并附证据）
- [ ] `pytest tests/ -q` 全绿
- [ ] 交付示例逐条可运行
- [ ] 没有未声明的网络调用（除文档明确的 API 调用点：GitHub API、LLM 调用）
- [ ] 无通过测试绕过（如跳过契约验证）
- [ ] README 已按文档要求生成（安装/Token 指引/示例输出/隐私说明/AI 免责）
- [ ] 代码量保持模块化精简（文档未标注行数上限，但禁止为了凑量写冗余代码）

---

## 接口核对清单（架构设计.md 第 5 节，验收关口——交付时逐条给证据）

- [ ] `store.get_cached` 24h 内不二次拉 API
- [ ] `rule_verdict` 对"2 年无 push + archived" 必返回非 None
- [ ] `unstar_many` 对所有就绪执行前必然调用 confirm_fn
- [ ] 两次 `unstar` 同批只改一次（幂等）
- [ ] `main.py` 无 `--yes` 时绝不发起 unstar 网络请求

---

## 附录：四份文档全文

### 思路.md

> 一句话：扫描你 GitHub 上所有 Star 的仓库，用 AI 告诉你"哪些已经死了、哪些值得看"——一键 unstar 死项目。

**一、项目思路**
1. **要解决的问题**：开发者 Star 仓库像收藏夹——只增不减。几年下来积累了 300-4000 个 Star，其中大量：项目已不再维护、已迁移新地址、作者弃坑、架构已过时。没有工具在做"该不该留"的判断，全是"收藏后遗忘"。
2. **调研证据（2026年8月实搜）**：直接竞品几乎为零（唯一同定位项目 Workaround 疑似 0★）；现有工具全是"收藏分类管理"（Astral、starverse、gh-stars-manager），不做废弃判断；唯一有点接近的 `gh-manager-cli` 也只是按时间过滤，无 AI 判断；GitHub 健康度打分器是红海（RepoScore 等），但"个人 Star 清理"是另一个场景，无人做。
3. **用户画像**：GitHub 重度用户（Star 数 >100）；技术博主/开源贡献者（需要筛选前沿项目）；想"清理收藏夹"但没动力手动翻的人。
4. **产品形态（MVP）**：Web 单页（纯前端）+ GitHub OAuth 或 Token 输入：输入 GitHub Token（只读权限）→ 拉取全部 starred 仓库（含 star 时间、stars 数、最后 push 时间、话题）→ AI 每仓给出 4 行意见："已死（2 年无提交）" / "活跃但被替代" / "值得关注" / "工具类常驻" → 打分列表：llm 建议 + 事实数据（last_pushed、open_issues_ratio）→ 勾选 → 一键 unstar（幂等，可回 star）。数据仅浏览器端处理（可选：后端 Python CLI 版）。
5. **核心杀手锏（差异化）**：① 决定性判断："该不该留"是唯一主张（现有产品都停在"帮你收藏"）；② 数据+理由：每个判断附证据（"last commit 2024-03，900 天无活动；作者已转向 X"）；③ 批量安全：unstar 前显示清单、按用户勾选执行、可间隔执行避免触发 GitHub 限流。
6. **边界与免责（低责任）**：只拉取公开数据；只读 Token（scope: `read:user` + 无需写权限做 unstar 用独立 API 调用）；unstar 是幂等且可逆（重新 star 回去原 star 数不变）；不删仓库不动代码；AI 判断仅供参考；投票式"可清理" vs "保留"由用户最终决定。

**二、技术选型**：语言 Python（纯本地 CLI）或 TypeScript（Web），双形态可选；GitHub 数据 PyGithub / octokit（官方库）；AI 判断一次 LLM 调用（结构化 JSON），本地亦可，key 自备；存储 SQLite 缓存 star 列表（避免每次拉 API）。**推荐起点：CLI 版（Python），Web 版后续剥离。**

**三、开发规范**（见 开发规范.md）。核心硬性要求：① Token 只存环境变量/.gitignore；绝不入日志或提交；② 拉取数据后先输出"待处理清单 + AI 理由"；unstar 必须逐项确认；③ 内置 GitHub 限流处理（60 req/h 未认证，用 Token 提升至 5000/h，批量 sleep 与重试）；④ 幂等：重复运行不会二次 unstar 已处理项；⑤ AI 判断 JSON 契约固定，判断失败该仓库跳过（不猜测默认值）。

**四、开发规划**（见 开发规划.md）：Phase 1 数据层 → Phase 2 AI 判断层 → Phase 3 执行层 → Phase 4 发布。

**五、判断标准**：对 1000 个 star 的项目 ≤60 秒扫描并输出完整报告；"已废弃"判断与人工抽查一致率 ≥85%（以 activation 数据为主，LLM 为辅）；全程只读+逐项确认，任何操作可撤销。

### 开发规范.md

**1. 总体约束**
- 语言：Python 3.9+（CLI 版）；结构单一文件可拆 4 模块
- 结构：`main.py`（CLI）/ `gh_data.py`（API 拉取）/ `analyze.py`（AI 判断）/ `actions.py`（执行）/ `store.py`（SQLite）
- 依赖：`PyGithub`、`openai`(可选)、`rich`、`sqlite3`（标准）
- 权限约束：`--token` 从环境变量 `GITHUB_TOKEN` 读取，绝不接受命令行明文

**2. 数据层**
- `gh_data`：`list_starred(username) -> [repo]`，每 repo 记录：`full_name, html_url, stars, language, pushed_at, archived, forked, description`
- 使用 `pagination`（默认 per_page=100；`starred_at` 可选）
- 缓存 SQLite 表 `stars`（键 `name` + `fetched_at`），**24 小时内不重复拉取**
- 限流：`rate_remaining` 检查；剩余 <100 时 sleep 到 reset（或提示 user 升权限）

**3. AI 判断层（接口契约）**
```
POST（LLM）输入：repo 摘要（stars/语言/pushed/archived/desc 前 200 字）
输出 JSON: {"verdict": "dead|stale|active|watch", "reason": "一句话理由",
            "suggest": "keep|unstar|revisit"}
```
- `verdict` 枚举严格固定；解析失败该仓标记 `unknown` 且**不执行任何动作**
- 补充规则（低优先，先跑确定性规则再回 LLM 增强）：`pushed_at` 超 730 天 → 候选 `dead`；`archived=True` → 候选 `dead`；`pushed_at` 90 天内 + stars 涨 → `active`
- 最终决策：规则确定性得分 + LLM 补充分数，人工在 CLI 输出中确认

**4. 执行层（安全硬约束）**
- `unstar` 用 `github.User.unstar()`（对应 API），**调用前必须打印整份清单**
- 支持组合：`--all`（全部按建议）、`--only dead`、`--only stale`
- **幂等**：已 unstar 的（SQLite 记录 `unstarred_at`）跳过并提示"已处理"
- 可回滚：`unstar` 操作反操作即重新 `star`（GitHub 支持，无需保留数据）
- 善意限速：批 unstar 每请求间 sleep 0.5s（避免触发 403）

**5. 输出**
- 终端表格：`[仓库] [stars] [last push] [verdict] [reason]`
- `--json 输出`（供程序化消费）
- 报告亦可保存 Markdown（`report.md`）便于分享

**6. 测试**
- `tests/test_gh_data.py`（mock PyGithub）：分页、缓存、限流跳过
- `tests/test_analyze.py`：JSON 契约、`dead/stale/active` 边界、解析失败处理（mock LLM 返回畸形 JSON 时优雅降级）
- `tests/test_actions.py`：unstar 幂等、unstar 记录
- `pytest` 全绿

**7. 交付物**
- CLI 可用 + README（安装/Token 指引/示例输出/隐私说明/AI 免责）
- 示例报告示例截图

### 开发规划.md

> 周期：2-3 天（AI 一次可冲刺）。先 CLI 后 Web。

- **Phase 1（半天）数据层**：`store.py` SQLite 建表 + 缓存逻辑；`gh_data.py` `list_starred` 分页 + `last_commit` 获取 + 限流检查；参数 `--user`（或 `--token` + 当前用户）、`--refresh`。验证：对有 100+ star 的演示账号跑一次，输出缓存命中与数据完整。
- **Phase 2（半天）AI 判断层**：`analyze.py` LLM 调用（`LLM_API_KEY` 环境变量）+ JSON 契约解析；规则前置 `dead/stale/active/archived` 硬规则；两路合并 + 最终 suggestion；`--dry-run` 输出全量报告不执行。验证：demo 报告 10 条仓库，人工抽查正确。
- **Phase 3（半天）执行层**：`actions.py` unstar/star、幂等、限速、日志；CLI 完整 `--dry-run` 默认，`--yes-dead`/`--yes-all` 才真执行；`--rollback`（重新 star 所选）。验证：测试账号 unstar 一组，确认可恢复。
- **Phase 4（1 天）Web 单页（可选）**：无后端 localStorage 存 token + fetch GitHub API + 同 LLM 判断；一键导出清单 / 重新导入；仅作演示增强（CLI 版已可独立使用）。
- **Phase 5（半天）发布**：README（Token 权限说明、免责）；`pyproject.toml` 包 + `gh-star-clean` 命令；GitHub 仓库 + 演示 gif。

**验收清单**：1000 star 账号跑 ≤ 60s（含 AI 判断）；报告包含每项的 `verdict + reason + 数据依据`；`unstar` 执行前二次清单确认；所有操作可撤销；无 Token 时给出安装/申请指引，不崩溃。

**风险与对策**：GitHub 限流 → 缓存 24h + 分页 + 限速 sleep；AI 误判"该死"的活跃仓库 → 表决机制：规则+LLM 双证据；报告给用户最终裁决；隐私（token）→ 只读权限 + 环境变量 + 不落库日志。

### 架构设计.md

> 目标：数据层/AI 层/执行层严格分离——AI 只做"判断"，执行只做"动作"，缓存只做"状态"。

**1. 总体分层**：`main.py`（CLI 编排）依赖 `gh_data`（GitHubAPI 读）/ `analyze`（规则+LLM 判断）/ `actions`（unstar/star）/ `store`（SQLite 缓存与状态）。依赖方向：`main → gh_data / analyze / actions`；`analyze → store`（读历史，防重复判断）；`actions → store`（写状态）。**禁止**：`gh_data` 内部不 import `analyze/actions`；LLM 密钥只进 `analyze`。

**2. 接口契约**

`gh_data.py`（只读数据源）：
```python
@dataclass
class RepoInfo:
    full_name: str
    html_url: str
    stars: int
    language: str | None
    pushed_at: datetime
    archived: bool
    open_issues: int
    description: str | None
    repo_created_at: datetime
    fetched_at: datetime

@dataclass
class FetchResult:
    repos: list[RepoInfo]
    rate_remaining: int
    rate_reset_at: datetime | None

def get_starred(username: str, token: str | None, *,
                per_page: int = 100) -> Iterator[RepoInfo]
    # 分页迭代；打印进度；Token 缺省 = 60/小时匿名（提示）

def fetch_error_classes() -> tuple[type[Exception], ...]
    # (GithubRateLimitError, GithubException) 统一异常出口
```

`store.py`（SQLite，唯一持久层）：
```python
SCHEMA = """
CREATE TABLE IF NOT EXISTS stars(
  full_name TEXT PRIMARY KEY,
  fetched_at INTEGER,           -- 缓存时间
  data_json TEXT,               -- RepoInfo 序列化（供离线重算）
  verdict TEXT,                 -- keep/unstar/revisit/null
  reason TEXT,
  score REAL,                   -- LLM/规则综合分 0-100
  unstarred_at INTEGER,         -- null=未处理
  decision_user TEXT            -- 'keep'|'unstar'|''（人工确认结果）
)
"""
class StarStore:
    def __init__(self, path: str = "~/.starclean.db")
    def get_cached(self, username) -> list[RepoInfo]      # fetched_at 24h 内
    def upsert_repos(self, repos: list[RepoInfo])
    def save_verdicts(self, verdicts: list[Verdict])     # verdict+reason+score
    def mark_unstarred(self, repo: str, at: datetime)
    def list_unstarred(self) -> list[str]
    def list_pending(self) -> list[str]
```

`analyze.py`（判断层，只读判定）：
```python
@dataclass
class Verdict:
    repo: str
    verdict: str            # "keep" | "unstar" | "review"
    score: int              # 0-100
    reason: str             # 一句话（含数据依据，如"last push 2.1y ago"）
    source: str             # "rule" | "llm"

# 本地硬规则（可离线！不依赖 LLM）：
RULE_EXPIRED_DAYS = 730      # >2年无活动 → 分 base 降低
RULE_ARCHIVED_DAY  = 1       # archived → unstar 得分升高
RULE_MIN_STARS_ACTIVE = 30   # <30 star 且 2年无 → 若无 forks 则 unstar

def rule_verdict(repo: RepoInfo) -> Verdict | None  # 输出确定性: keep/unstar/审查
def llm_verdict(repo: RepoInfo) -> Verdict           # 调用外部模型（JSON契约见下）
def combined(repo: RepoInfo) -> Verdict              # 规则优先；规则 None 时走 llm
def summarize(candidates: list[Verdict]) -> str      # 生成 Markdown 报告
```

**LLM JSON 契约**（非严格结构重试一次）：
```json
{"score": 32, "verdict": "unstar", "reason": "最后提交 2024-03，900 天无活动且无分支; 建议清理"}
```
`verdict ∈ {"keep","unstar","revisit"}`，`score∈[0,100]`。

`actions.py`（唯一写 GitHub 处）：
```python
class ActionError(Exception)

def unstar(repo: str, token: str, *, dry_run: bool = True) -> bool
def restar(repo: str, token: str) -> bool                 # rollback
def unstar_many(repos: list[str], token, *, batch_size=5, sleep_s=0.5,
                confirm_fn: Callable[[list[str]], bool]) -> dict[str, str]
    # {repo: "done"|"skipped"(已处理)|"failed"|"cancelled"}
```
- `unstar_many` 先调 `confirm_fn(清单)` 展示全量→用户确认→执行
- 幂等：`store.list_unstarred()` 内跳过
- 限速：每批 sleep 0.5s；403 时急停并提示

CLI 契约：
```
starclean scan [--user u] [--token env:GITHUB_TOKEN] [--refresh] [--no-llm] [--json]
starclean report [--to report.md]
starclean decide? unstar --dead|--stale|--all [--yes] [--dry-run]
starclean undo [--repo x]
```
- 默认 `scan + report` 只读；`unstar` 必须有 `--yes` 或交互确认

**3. 数据流**
```
scan: gh_data.get_starred → store.upsert(缓存更新)
   → for each：combined()→ store.apply_verdicts
   → 输出 shortlist（按 score 升序）→ 命令决定是否 unstar
unstar: args筛选 → 展示确认 → unstar_many → store.mark_unstarred
rollback: list_unstarred → restar → 清除标记
```

**4. 并行开发切分**：A `gh_data.py`+测试（无依赖）；B `store.py`+测试（无依赖）；C `actions.py`+测试（依赖 B 桩）；D `analyze.py` 规则模型+LLM（依赖 A 类型、B）；E `main.py` CLI 集成（依赖 A-D）。A/B 并行 → C/D 并行 → E。

**5. 接口核对清单**
- [ ] `store.get_cached` 24h 内不二次拉 API
- [ ] `rule_verdict` 对"2 年无 push + archived" 必返回非 None
- [ ] `unstar_many` 对所有就绪执行前必然调用 confirm_fn
- [ ] 两次 `unstar` 同批只改一次（幂等）
- [ ] `main.py` 无 `--yes` 时绝不发起 unstar 网络请求
