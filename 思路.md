# Star 仓库清理建议器（StarGrave）

> 一句话：扫描你 GitHub 上所有 Star 的仓库，用 AI 告诉你"哪些已经死了、哪些值得看"——一键 unstar 死项目。

## 一、项目思路

### 1. 要解决的问题
开发者 Star 仓库像收藏夹——只增不减。几年下来积累了 300-4000 个 Star，其中大量：项目已不再维护、已迁移新地址、作者弃坑、架构已过时。没有工具在做"该不该留"的判断，全是"收藏后遗忘"。

### 2. 调研证据（2026年8月实搜）
- 直接竞品几乎为零：唯一同定位项目（Workaround）疑似 0★
- 现有工具全是"收藏分类管理"（Astral、starverse、gh-stars-manager），**不做废弃判断**
- 唯一有点接近的 `gh-manager-cli` 也只是按时间过滤，无 AI 判断
- GitHub 健康度打分器是红海（RepoScore 等），但"个人 Star 清理"是另一个场景，无人做

### 3. 用户画像
- GitHub 重度用户（Star 数 >100）
- 技术博主/开源贡献者（需要筛选前沿项目）
- 想"清理收藏夹"但没动力手动翻的人

### 4. 产品形态（MVP）
Web 单页（纯前端）+ GitHub OAuth 或 Token 输入：
```
输入 GitHub Token（只读权限）
→ 拉取全部 starred 仓库（含 star 时间、stars 数、最后 push 时间、话题）
→ AI 每仓给出 4 行意见：
  "已死（2 年无提交）" / "活跃但被替代" / "值得关注" / "工具类常驻"
→ 打分列表：llm 建议 + 事实数据（last_pushed、open_issues_ratio）
→ 勾选 → 一键 unstar（幂等，可回 star）
```
数据仅浏览器端处理（可选：后端 Python CLI 版）。

### 5. 核心杀手锏（差异化）
1. **决定性判断**："该不该留"是唯一主张（现有产品都停在"帮你收藏"）
2. **数据+理由**：每个判断附证据（"last commit 2024-03，900 天无活动；作者已转向 X"）
3. **批量安全**：unstar 前显示清单、按用户勾选执行、可间隔执行避免触发 GitHub 限流

### 6. 边界与免责（低责任）
- 只拉取公开数据；只读 Token（scope: `read:user` + 无需写权限做 unstar 用独立 API 调用）
- unstar 是幂等且可逆（重新 star 回去原 star 数不变）；不删仓库不动代码
- AI 判断仅供参考；投票式"可清理" vs "保留"由用户最终决定

## 二、技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | Python（纯本地 CLI）或 TypeScript（Web） | 双形态可选 |
| GitHub 数据 | PyGithub / octokit | 官方库 |
| AI 判断 | 一次 LLM 调用（结构化 JSON），本地亦可 | key 自备 |
| 存储 | SQLite 缓存 star 列表（避免每次拉 API） | 轻量 |

推荐起点：**CLI 版（Python）**，Web 版后续剥离。

## 三、开发规范

见 `开发规范.md`。核心硬性要求：
1. Token 只存环境变量/.gitignore；绝不入日志或提交
2. 拉取数据后先输出"待处理清单 + AI 理由"；unstar 必须逐项确认
3. 内置 GitHub 限流处理（60 req/h 未认证，用 Token 提升至 5000/h，批量 sleep 与重试）
4. 幂等：重复运行不会二次 unstar 已处理项
5. AI 判断 JSON 契约固定，判断失败该仓库跳过（不猜测默认值）

## 四、开发规划（见 开发规划.md）

Phase 1 数据层 → Phase 2 AI 判断层 → Phase 3 执行层 → Phase 4 发布。

## 五、判断标准
- 对 1000 个 star 的项目 ≤60 秒扫描并输出完整报告
- "已废弃"判断与人工抽查一致率 ≥85%（以 activation 数据为主，LLM 为辅）
- 全程只读+逐项确认，任何操作可撤销