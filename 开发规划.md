# 开发规划 — Star 仓库清理建议器

> 周期：2-3 天（AI 一次可冲刺）。先 CLI 后 Web。

## Phase 1（半 天）：数据层
- [ ] `store.py`：SQLite 建表 + 缓存逻辑
- [ ] `gh_data.py`：`list_starred` 分页 + `last_commit` 获取 + 限流检查
- [ ] 参数：`--user`（或 `--token` + 当前用户）、`--refresh`
- 验证：对有 100+ star 的演示账号跑一次，输出缓存命中与数据完整

## Phase 2（半天）：AI 判断层
- [ ] `analyze.py`：LLM 调用（`LLM_API_KEY` 环境变量）+ JSON 契约解析
- [ ] 规则前置：`dead/stale/active/archived` 硬规则
- [ ] 两路合并 + 最终 suggestion
- [ ] `--dry-run`：输出全量报告不执行
- 验证：demo 报告 10 条仓库，人工抽查正确

## Phase 3（半天）：执行层
- [ ] `actions.py`：unstar/star、幂等、限速、日志
- [ ] CLI 完整：`--dry-run` 默认，`--yes-dead`/`--yes-all` 才真执行
- [ ] `--rollback`（重新 star 所选）
- 验证：测试账号 unstar 一组，确认可恢复

## Phase 4（1 天）：Web 单页（可选）
- [ ] 无后端：localStorage 存 token + fetch GitHub API + 同 LLM 判断
- [ ] 一键导出清单 / 重新导入
- [ ] 仅作演示增强（CLI 版已可独立使用）

## Phase 5（半天）：发布
- [ ] README（酸 Token 权限说明、免责）
- [ ] `pyproject.toml` 包 + `gh-star-clean` 命令
- [ ] GitHub 仓库 + 演示 gif

## 验收清单
- [ ] 1000 star 账号跑 ≤ 60s（含 AI 判断）
- [ ] 报告包含每项的 `verdict + reason + 数据依据`
- [ ] `unstar` 执行前二次清单确认；所有操作可撤销
- [ ] 无 Token 时给出安装/申请指引，不崩溃

## 风险与对策
| 风险 | 对策 |
|---|---|
| GitHub 限流 | 缓存 24h + 分页 + 限速 sleep |
| AI 误判"该死"的活跃仓库 | 表决机制：规则+LLM 双证据；报告给用户最终裁决 |
| 隐私（token） | 只读权限 + 环境变量 + 不落库日志 |